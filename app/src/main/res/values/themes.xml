<resources>
    <!-- Base application theme for Material3 -->
    <style name="Base.Theme.BanHangSo" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/boston_blue</item>
        <item name="colorOnPrimary">@color/white</item>

        <item name="colorSecondary">@color/mariner</item>
        <item name="colorOnSecondary">@color/white</item>

        <item name="colorError">@color/color_warning</item>
        <item name="android:textColor">@color/raven</item>
        <item name="android:colorBackground">@color/alto</item>

        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!--     Other UI component styles -->
    <style name="RoundedBorderShapeableImage">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">8dp</item>
    </style>

    <style name="CircleImage">
        <item name="cornerSize">50%</item>
    </style>

</resources>
